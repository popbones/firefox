[precommitHandler-redirect-throws.html]
  expected:
    if (os == "linux") and not debug and asan and fission: [CRASH, TIMEOUT, OK]
    if (os == "linux") and not debug and not asan and (os_version == "24.04"): [OK, TIMEOUT, CRASH]
    if (os == "win") and debug and (processor == "x86_64"): [OK, CRASH, TIMEOUT]
    if (os == "win") and debug and (processor == "x86"): [ERROR, CRASH, OK, TIMEOUT]
    if (os == "win") and not debug and (processor == "x86"): [OK, TIMEOUT, CRASH]
    if (os == "mac") and not debug: [OK, TIMEOUT, CRASH]
    if os == "android": [OK, TIMEOUT, CRASH]
    [CRASH, OK, TIMEOUT]
  [redirect() after finish]
    expected: [PASS, FAIL, TIMEOUT]

  [redirect() after commit]
    expected: [PASS, FAIL, TIMEOUT]

  [redirect() traverse]
    expected:
      if asan and fission: [TIMEOUT, PASS, FAIL]
      [FAIL, PASS, TIMEOUT]
